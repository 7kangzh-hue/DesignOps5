
import React from 'react';

// This component is deprecated and has been split into ManagerNotes and SmartReport.
// Keeping file/export to avoid build errors if referenced elsewhere temporarily.
export const ManagerDashboard: React.FC = () => {
  return null;
};
